#
# Multiconfig for the Pixhawk 6X's PX4IO coprocessor build
# (px4_io-v2_default, Cortex-M3) -- a separate multiconfig from
# pixhawk6x because it's a different MACHINE/tune entirely.
# specs/002-px4-firmware.md section 5.4, REQ-4.
#
MACHINE = "pixhawk-6x-io"
TCLIBC = "baremetal"
